const testData = {
  themeName: 'UI test theme - Performance tests',
  maxImportWaitTimeMillis: 2 * 60 * 60 * 1000,
  maxPublishingWaitTimeMillis: 2 * 60 * 60 * 1000,
} as const;

export default testData;
