import { PublicationTitle } from '@common/services/publicationService';

const testPublicationTitle: PublicationTitle = {
  publicationId: 'publication-1',
  title: 'Test Publication Title',
};

export default testPublicationTitle;
