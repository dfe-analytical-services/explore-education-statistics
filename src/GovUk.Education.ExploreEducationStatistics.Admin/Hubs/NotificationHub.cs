using Microsoft.AspNetCore.SignalR;

namespace GovUk.Education.ExploreEducationStatistics.Admin.Hubs;

public class NotificationHub : Hub
{
    // Intentionally blank - this hub is currently only used externally via an IHubContext, but a class is required for hub mapping in Startup.cs
}
