/**
 * Maximum length of a data set title, shared by the upload form and the
 * edit title modal so the two cannot drift apart.
 */
const dataFileTitleMaxLength = 120;

export default dataFileTitleMaxLength;
